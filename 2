A file max-temp.text is craeted using cat command and the values(Date,temperature) was stored in them later saved.And the screen shot was
stored in two files 2.1 and 2.2


Using the put command the hadoop file is copied.and it is stored in screenshot 2.3


And by using chmod the read , write and executing permission will be provided to the owner as well group participants and others could only
write.so binary to digital conersion takes place i fall the three permissions were available then (111=7) so 7,7,4.

